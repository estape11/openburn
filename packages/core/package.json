{
  "name": "@openburn/core",
  "version": "0.0.1",
  "private": true,
  "description": "Pure TypeScript CAM core: SVG parsing, intermediate representation, path planning, G-code generation. No I/O — runs in Node and the browser.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  }
}

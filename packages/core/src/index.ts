// @openburn/core — pure CAM core. No Node APIs, no I/O (must run in browser).
// Structure lands with v0.1.0 issues: ir/, svg/, geometry/, planner/, emit/.

export { formatCoord } from './emit/format.js';

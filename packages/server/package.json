{
  "name": "@openburn/server",
  "version": "0.0.1",
  "private": true,
  "description": "OpenBurn server: device transports (TCP), GRBL protocol, HTTP/WebSocket API, GRBL simulator.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "dev": "echo 'Not implemented yet — server lands with #15 (see ROADMAP.md v0.1.0)' && exit 1",
    "sim": "tsx src/sim/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.9",
    "tsx": "^4.19.1"
  }
}
